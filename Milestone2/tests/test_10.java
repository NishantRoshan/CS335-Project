public class operators {
    public static void main() {
        int a,b,c,d,e,f;
        float g;
        double h, i, j;
        boolean k,l;
        a = b + c * d - (e - f) /b + a;
        g = a + g;
        h = i + (j - h) * i;
        k = k && l || k;
        return 0;
    }
}
